# Claude Rule: Foundation

- Always plan first for non-trivial work.
- Keep files within size limits.
- Do not duplicate logic.
- Respect monorepo boundaries (apps only import packages; packages never import apps).
- Prefer referencing `docs/ai/*` instead of duplicating long guidance.
