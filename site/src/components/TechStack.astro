---
import SectionHeading from "./SectionHeading.astro"

const stack = [
  { layer: "Frontend", tech: "Next.js 15, React 19, Tailwind CSS 4, shadcn/ui, Radix UI", color: "accent" },
  { layer: "Charts", tech: "TradingView Lightweight Charts 5", color: "green" },
  { layer: "Backend", tech: "Node.js daemon (custom HTTP + WebSocket server)", color: "green" },
  { layer: "Edge", tech: "Cloudflare Workers + Durable Objects", color: "amber" },
  { layer: "Database", tech: "SQLite via Prisma ORM (LibSQL adapter)", color: "blue" },
  { layer: "AI Runtime", tech: "Claude API (Anthropic SDK)", color: "indigo" },
  { layer: "AI Dev Tool", tech: "Claude Code with governance system", color: "indigo" },
  { layer: "Monorepo", tech: "pnpm workspaces + Turborepo", color: "accent" },
  { layer: "Testing", tech: "Vitest with coverage", color: "green" },
  { layer: "Language", tech: "TypeScript 5.7 (strict mode)", color: "blue" },
]

const colorMap: Record<string, string> = {
  accent: "text-accent",
  green: "text-green",
  amber: "text-amber",
  blue: "text-blue",
  indigo: "text-indigo",
}
---

<section class="border-t border-border bg-bg-surface py-24" aria-label="Tech stack">
  <div class="mx-auto max-w-6xl px-6">
    <SectionHeading title="Tech Stack" subtitle="Modern, production-grade tools across every layer." />

    <div class="mx-auto max-w-3xl">
      <div class="overflow-hidden rounded-xl border border-border">
        {
          stack.map((item, i) => (
            <div
              class:list={[
                "flex items-center gap-4 px-6 py-4",
                i < stack.length - 1 && "border-b border-border",
                i % 2 === 0 ? "bg-bg-surface" : "bg-bg-elevated",
              ]}
            >
              <span class:list={["w-28 shrink-0 text-sm font-semibold", colorMap[item.color]]}>{item.layer}</span>
              <span class="text-sm text-text-muted">{item.tech}</span>
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>
