name: Documentation

# Changelog generation has moved to .github/workflows/release.yml
# This workflow is kept for future doc-specific automation (e.g. TypeDoc sync)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Documentation workflow — see release.yml for changelog generation"
